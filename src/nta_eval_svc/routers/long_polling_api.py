"""Long polling API router skeleton."""
from fastapi import APIRouter

long_polling_api_router = APIRouter(prefix="/long-poll")

# No endpoints yet; skeleton router to be expanded with long polling handlers
__all__ = ["long_polling_api_router"]
